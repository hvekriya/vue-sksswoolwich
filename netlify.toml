# netlify.toml
[build]
  command = "npm run generate" # or "yarn build"
  publish = "dist"          # or ".nuxt/dist" depending on your Nuxt static output path

[functions]
  directory = "netlify/functions"